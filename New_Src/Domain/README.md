# 🏛️ Isatis ICP - Domain Layer

Core entities and business rules.

## 📋 Overview

این لایه شامل Entity های اصلی برنامه است که مستقل از هر framework هستند.

## 📁 Structure

```
Domain/
└── Entities/
    ├── User.cs              # کاربران سیستم
    ├── Project.cs           # پروژه‌های آزمایشگاهی
    ├── RawDataRow.cs        # داده‌های خام ورودی
    ├── ProcessedData.cs     # داده‌های پردازش شده
    ├── ProjectState.cs      # وضعیت/snapshot پروژه
    ├── CrmData.cs           # Reference Materials
    └── ChangeLog.cs         # لاگ تغییرات
```

## 📊 Entity Relationships

```
┌─────────────┐       ┌─────────────────┐
│    User     │       │     Project     │
├─────────────┤       ├─────────────────┤
│ UserId (PK) │       │ ProjectId (PK)  │
│ Username    │       │ ProjectName     │
│ Email       │       │ Owner           │
│ Role        │       │ CreatedAt       │
└─────────────┘       └────────┬────────┘
                               │
           ┌───────────────────┼───────────────────┐
           ▼                   ▼                   ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   RawDataRow    │  │  ProcessedData  │  │  ProjectState   │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│ Id (PK)         │  │ Id (PK)         │  │ StateId (PK)    │
│ ProjectId (FK)  │  │ ProjectId (FK)  │  │ ProjectId (FK)  │
│ ColumnData      │  │ Data            │  │ Data            │
│ SampleId        │  │ AnalysisType    │  │ Description     │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

## 🔐 User Roles

```csharp
public static class UserRoles
{
    public const string Admin = "Admin";
    public const string Analyst = "Analyst";
    public const string Viewer = "Viewer";
}
```